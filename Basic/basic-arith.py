a = input("Enter a value:")
b = input("Enter another value:")

add = float(a) + float(b)
print("Addition of two values are:",add)

sub = float(a) - float(b)
print("Substraction of two values are:",sub)

mul = float(a) * float(b)
print("Multiplicaton of two values are:",mul)

div = float(a) / float(b)
print("Divition of two values are:",div)

modu = float(a) % float(b)
print("Modulus of two values are:",modu)

fdiv = float(a) // float(b)
print("Floor Division of two values are:",fdiv)

expo = float(a) ** float(b)
print("Exponent of two values are:",expo)
